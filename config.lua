Config = {}

-----------------------------------------------------------
-- 車両警告 / Vehicle warning
--   指定した車両に乗っている間、画面にテキストを描画する。
--   描画はネイティブの DrawText で行う（NUI は使わない）。
-----------------------------------------------------------
Config.Vehicle = {
    -- この機能を使わない場合は false にする（スレッド自体が起動しなくなる）
    enabled = true,

    -- 表示するメッセージ
    text = 'この車両は犯罪利用禁止です',

    -- 文字色 { R, G, B, A }
    textColor = { 255, 50, 50, 255 },

    -- 文字サイズ
    textScale = 0.5,

    -- 画面上の表示位置 (0.0〜1.0)
    position = { x = 0.01, y = 0.01 },

    -- 対象車両のスポーン名。GetHashKey による完全一致で照合するため、
    -- 派生車種（police4 など）を含めたい場合は個別に列挙すること。
    vehicles = {
        'police',
        'police2',
        'ambulance',
        'pbus',
    },
}

-----------------------------------------------------------
-- 武器警告 / Weapon warning
--   指定した武器を手に持っている間、NUI でテキストを表示する。
--   見た目（色・位置・フォント）は html/index.html の CSS で編集する。
-----------------------------------------------------------
Config.Weapon = {
    -- この機能を使わない場合は false にする（スレッド自体が起動しなくなる）
    enabled = true,

    -- キーは武器ハッシュ。バッククォートはコンパイル時にハッシュへ変換される。
    --
    -- 既定はバニラの制限武器にしてある（導入直後に動作を確認できるようにするため）。
    --
    -- ⚠ アドオン武器を対象にする場合の注意:
    --   その武器を提供するリソースが導入されていないとハッシュが解決されず、
    --   エラーも警告も出ないまま**無言で無視される**。
    --   「表示されない」ときは、まず対象武器が実際に装備できるかを確認すること。
    weapons = {
        [`WEAPON_RPG`] = {
            text = 'この武器はサーバールールで使用が制限されています',
        },
        [`WEAPON_MINIGUN`] = {
            text = 'この武器はサーバールールで使用が制限されています',
        },
        -- アドオン武器の例（該当する武器リソースを導入している場合のみ機能する）
        -- [`WEAPON_BANANA`] = {
        --     text = '食べ物ではありません武器です。取り扱いに注意してください。',
        -- },
    },
}
